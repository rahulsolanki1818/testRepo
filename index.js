function test() {
  let changes = 0;

  if (changes) {
    console.log("changes occured in git");
  } else {
    console.log("no changes occured in git.");
  }
}

function test() {
  let changes = 0;

  if (changes) {
    console.log("changes occured in git");
  } else {
    console.log("no changes occured in git.");
  }
}

function test() {
  let changes = 0;

  if (changes) {
    console.log("changes occured in git");
  } else {
    console.log("no changes occured in git.");
  }
}

function test() {
  let changes = 0;

  if (changes) {
    console.log("changes occured in git");
  } else {
    console.log("no changes occured in git.");
  }
}
